export { IdentityProvider, useIdentity } from './IdentityContext';
export { saveIdentity, loadIdentity, deleteIdentity, loadContacts, saveContact, deleteContact } from './store';
export type { Identity, StoredContact } from './types';
